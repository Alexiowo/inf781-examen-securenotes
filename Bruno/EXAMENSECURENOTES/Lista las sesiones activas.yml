info:
  name: "Lista las sesiones activas "
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/auth/sessions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
