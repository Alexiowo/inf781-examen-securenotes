info:
  name: Lista únicamente las notas del usuario
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/notes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
